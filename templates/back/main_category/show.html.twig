{% extends "back/base.html.twig" %}


{% block title %}MainCategory{% endblock %}

{% block body %}
    <h1>MainCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ main_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ main_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('main_category_index') }}">back to list</a>

    <a href="{{ path('main_category_edit', {'id': main_category.id}) }}">edit</a>

    {{ include('back/main_category/_delete_form.html.twig') }}
{% endblock %}
